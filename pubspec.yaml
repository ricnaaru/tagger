name: tagger
description: Flutter tag inputter
version: 0.0.1
homepage: https://github.com/ricnaaru/
repository: https://github.com/ricnaaru/tagger

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

  characters: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.dreamob.tagger
        pluginClass: TaggerPlugin
      ios:
        pluginClass: TaggerPlugin